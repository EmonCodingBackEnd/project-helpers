server:
  port: 8771

spring:
  rabbitmq:
    host: ${dynamic.host}
    port: 5672

eureka:
  client:
    service-url:
      defaultZone: http://${dynamic.host}:8761/eureka/
  instance:
    prefer-ip-address: true
